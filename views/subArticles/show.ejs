<!DOCTYPE html>
<html lang="en">

<head>
    <title>Sub Article</title>
    <%- include('../common/head') %>
</head>

<body>
    <div class="container">

        <h1>
            SUB ARTICLE !
        </h1>

        <div class="mb-2">
            <a href="/" class="btn btn-secondary">All Articles</a>
            <a href="/articles/<%= article.slug %>" class="btn btn-secondary">Parent Article</a>
            <a href="/articles/subarticle/edit/<%= subArticle.id %>" class="btn btn-primary">Edit</a>
            <form action="/articles/subarticle/<%= subArticle.id %>?_method=DELETE" method="POST" class="d-inline">
                <button class="btn btn-danger" type="submit">Delete</button>
            </form>
        </div>


        <h3>
            All subarticles: <%= article.subArticles.length %>
                <br />
                subArticleIndex = <%= subArticleIndex %>
        </h3>


        <h2>
            <%- subArticle.sanitizedHtml %>
        </h2>

        <% if (subArticle.coverImageName != null) { %>
            <figure class="figure w-100 text-center">
                <img class="figure-img img-fluid rounded"
                    src="<%= article.coverImageFolderPath + subArticle.coverImageName %>">
            </figure>
        <% } %>

        <div class="row">
            <div class="col-4">
                <a href="<%= locals.prevBtnUrl %>" class="btn btn-lg btn-secondary w-100">
                    < Prev </a>
            </div>
            <div class="col-8">
                <a href="<%= locals.nextBtnUrl %>" class="btn btn-lg btn-primary w-100">
                    Next >
                </a>
            </div>
        </div>

    </div>
</body>

</html>